"use client";

import React from 'react';

// Lightweight QuickActions placeholder to satisfy AdminPageShell import.
// If a richer implementation exists, replace this file with the intended component.
export default function QuickActions() {
  return (
    <div className="p-4 rounded-xl border bg-white dark:bg-slate-800 border-gray-200 dark:border-slate-700">
      <h4 className="text-sm font-medium">Acciones rápidas</h4>
      <p className="text-xs text-slate-500 mt-2">Accesos directos y acciones del dashboard.</p>
    </div>
  );
}
